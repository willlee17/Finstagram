import { StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  headerContainer: {
    flexDirection: 'row',
  },
  name: {
    alignSelf: "center",
    fontWeight: "bold",
    color: "#3c3c3c", 
  }
})

export default styles;