import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  image: {
    borderRadius: 40,
    borderWidth: 1,
    borderColor: "#fff"
  },
  viewContainer: {
    margin: 5,
    borderRadius: 40,
    borderWidth: 3,
    borderColor: "#ae22e0"
  }
})

export default styles;